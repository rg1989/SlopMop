---
phase: 06-slop-config-vault
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/useSessionManager.test.ts
  - tests/gsd.test.ts
  - tests/useSettings.test.ts
  - tests/FolderPicker.test.tsx
  - tests/App.slopOnboarding.test.tsx
  - tests/VaultTab.test.tsx
  - server/gsd.ts
  - client/hooks/useSessionManager.ts
autonomous: true
requirements:
  - BUG-01
  - BUG-02

must_haves:
  truths:
    - "Only one tab is created on fresh load even if spawn() is called twice with initial:true"
    - "Phases without a ## Phases overview entry still appear in parseRoadmapMd output"
    - "Wave 0 test stubs exist for all new modules and are RED before implementation"
  artifacts:
    - path: "tests/gsd.test.ts"
      provides: "parseRoadmapMd behavior tests"
      exports: []
    - path: "tests/useSettings.test.ts"
      provides: "server-backed settings tests"
      exports: []
    - path: "tests/FolderPicker.test.tsx"
      provides: "recent paths via server endpoint tests"
      exports: []
    - path: "tests/App.slopOnboarding.test.tsx"
      provides: "slop-status check on cwd change tests"
      exports: []
    - path: "tests/VaultTab.test.tsx"
      provides: "vault status display and backup/restore tests"
      exports: []
    - path: "server/gsd.ts"
      provides: "rewritten parseRoadmapMd using detail sections as primary source"
      contains: "Pass 1"
    - path: "client/hooks/useSessionManager.ts"
      provides: "dedup guard for initial:true spawn"
      contains: "name === ''"
  key_links:
    - from: "client/hooks/useSessionManager.ts spawn()"
      to: "existing session with name === ''"
      via: "early return before crypto.randomUUID()"
      pattern: "current.find\\(s => s.name === ''\\)"
    - from: "server/gsd.ts parseRoadmapMd"
      to: "### Phase N: detail sections"
      via: "Pass 2 direct build"
      pattern: "phases.push\\(currentPhase\\)"
---

<objective>
Wave 0 test stubs for all new modules in Phase 6, plus two bug fixes that unblock all downstream plans.

Purpose: Bug fixes ship first so they don't linger; stub tests establish RED baselines so every subsequent plan has an automated verify gate from the start.
Output: 6 new/extended test files (RED or passing for bug fixes), rewritten parseRoadmapMd, dedup guard in spawn().
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-slop-config-vault/06-CONTEXT.md
@.planning/phases/06-slop-config-vault/06-RESEARCH.md
@.planning/phases/06-slop-config-vault/06-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. -->

From server/gsd.ts (current, to be rewritten):
```typescript
export type PhasePlan = { file: string; name: string; completed: boolean };
export type PhaseDraft = { number: number; name: string; goal: string; completed: boolean; plans: PhasePlan[] };
export function parseRoadmapMd(content: string): PhaseDraft[]
```

From client/hooks/useSessionManager.ts spawn() (current, to be patched, lines 83-106):
```typescript
const spawn = useCallback((cwd: string, { initial = false }: { initial?: boolean } = {}): string => {
  const current = sessionsRef.current;
  if (current.length >= MAX_SESSIONS) { return activeIdRef.current ?? current[current.length - 1]?.id ?? ''; }
  // FIX GOES HERE — before the !initial block
  if (!initial) {
    const unnamedTab = current.find(s => s.name === 'New');
    if (unnamedTab) { setActiveId(unnamedTab.id); return unnamedTab.id; }
  }
  const id = crypto.randomUUID();
  const name = initial ? '' : 'New';
  ...
}, []);
```

From tests/useSessionManager.test.ts (existing — extend, don't replace):
Already has tests for spawn(), close(), updateName(), updateStatus().
Extend with a new describe block: "dedup guard for initial spawn".

From tests/OnboardingModal.test.tsx (existing — extend, don't replace):
Already has tests for OnboardingModal visibility. Will be extended in plan 06-03.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bug fixes — double-spawn dedup + parseRoadmapMd rewrite</name>
  <files>client/hooks/useSessionManager.ts, server/gsd.ts, tests/useSessionManager.test.ts, tests/gsd.test.ts</files>
  <behavior>
    useSessionManager.test.ts — new describe block "dedup guard for initial spawn":
    - Test: calling spawn(cwd, {initial:true}) twice returns the same id (the second call finds the session with name === '' and returns it, no new session created)
    - Test: sessions.length === 1 after two initial spawns
    - Test: non-initial spawn(cwd) does not trigger the initial dedup path

    gsd.test.ts — new file, describe "parseRoadmapMd":
    - Test: phase with no ## Phases list entry is still parsed when it has a ### Phase N: section
    - Test: goal from **Goal**: line is captured
    - Test: completed state comes from ## Phases checkbox (- [x]) not from ### Phase N: presence
    - Test: plans list from Plans: block is parsed correctly
    - Test: phases returned sorted by number ascending
    - Test: existing ROADMAP.md structure (phases IN the ## Phases list) still parses correctly
  </behavior>
  <action>
    STEP 1 — Write failing tests first.

    Extend tests/useSessionManager.test.ts with a new describe block at the bottom. Use the existing mock pattern (vi.mock + sessionsRef via returned spawn callback). The test calls spawn twice with initial:true and asserts sessions has length 1 after both calls. Read existing test file patterns before adding — do not rewrite the file.

    Create tests/gsd.test.ts. Import parseRoadmapMd from '../server/gsd.js'. Write describe("parseRoadmapMd") with 6 tests covering: detail-section-first parsing, goal extraction, completed-from-overview, plans list, sorted output, backward compat.

    Run npm test -- --run to confirm RED (spawn dedup and gsd tests fail).

    STEP 2 — Fix useSessionManager.ts spawn().
    Insert after the MAX_SESSIONS guard and before the !initial block (around line 89):
    ```typescript
    if (initial) {
      const existing = current.find(s => s.name === '');
      if (existing) {
        setActiveId(existing.id);
        return existing.id;
      }
    }
    ```

    STEP 3 — Rewrite parseRoadmapMd in server/gsd.ts.
    Replace the existing function body (lines 22-56) with the two-pass algorithm:
    Pass 1: scan ## Phases list → build completedMap: Map<number, boolean>
    Pass 2: scan ### Phase N: detail sections → build phases array directly, look up completedMap for completed status
    Return phases.sort((a, b) => a.number - b.number).
    Keep all other exports (PhasePlan, PhaseDraft, parseStateMd, patchRoadmapRemovePlan) unchanged.

    Run npm test -- --run to confirm GREEN for useSessionManager and gsd tests.
  </action>
  <verify>
    <automated>npm test -- --run useSessionManager && npm test -- --run gsd</automated>
  </verify>
  <done>
    - spawn(cwd, {initial:true}) called twice produces exactly 1 session
    - parseRoadmapMd returns phases that exist only in ### Phase N: detail sections
    - All existing useSessionManager tests still pass
    - All gsd tests green
  </done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 stubs for new client modules</name>
  <files>tests/useSettings.test.ts, tests/FolderPicker.test.tsx, tests/App.slopOnboarding.test.tsx, tests/VaultTab.test.tsx</files>
  <action>
    Create 4 new test stub files. Each file uses @ts-expect-error on imports that don't exist yet so Vitest runs RED without TypeScript blocking compilation. See [Phase 02-file-system] and [Phase 04-multi-session-tabs] decisions in STATE.md — same pattern used successfully.

    tests/useSettings.test.ts:
    - Import useSettings from '../client/hooks/useSettings' (real, exists)
    - Stub tests (not yet implemented behavior):
      - "loads settings from server on mount, overwriting localStorage"
      - "falls back to localStorage if server returns null"
      - "PUT /api/global-settings called on update()"
      - "migration: writes localStorage value to server if server returns null"
    - Use vi.stubGlobal('fetch', ...) to mock fetch responses
    - Mark tests with todo() or placeholder expect(true).toBe(false) so they are RED

    tests/FolderPicker.test.tsx:
    - Import FolderPicker from '../client/components/FolderPicker' (real, exists)
    - Stub test: "loads recent paths from GET /api/recent-paths on mount"
    - Stub test: "calls PUT /api/recent-paths when a folder is connected"
    - Mark RED with placeholder assertions

    tests/App.slopOnboarding.test.tsx:
    - Use @ts-expect-error on any App import if App.tsx doesn't export named — or test the behavior via mocked fetch
    - Stub test: "fetches /api/slop-status when cwd changes"
    - Stub test: "OnboardingModal renders when slopExists is false"
    - Stub test: "OnboardingModal does not render when slopExists is true"
    - Mark RED with placeholder assertions

    tests/VaultTab.test.tsx:
    - // @ts-expect-error
    - import { VaultTab } from '../client/components/VaultTab';
    - Stub test: "renders a list of backup targets from vault-status data"
    - Stub test: "Backup All button calls POST /api/vault-backup"
    - Stub test: "Restore button calls POST /api/vault-restore with target id"
    - Mark RED with placeholder assertions

    Run npm test -- --run to confirm all 4 new files are RED (import errors or placeholder failures). Existing tests must remain green.
  </action>
  <verify>
    <automated>npm test -- --run useSettings && npm test -- --run FolderPicker && npm test -- --run VaultTab</automated>
  </verify>
  <done>
    - 4 test stub files exist
    - useSettings, FolderPicker, App.slopOnboarding, VaultTab test files all run (RED is acceptable)
    - Existing test suite still green (no regressions)
  </done>
</task>

</tasks>

<verification>
npm test -- --run
All tests pass or are intentionally RED stubs. No new test regressions in existing suite.
</verification>

<success_criteria>
- spawn(cwd, {initial:true}) idempotent — two calls produce one session
- parseRoadmapMd returns phases from ### Phase N: sections even without ## Phases list entry
- 4 new Wave 0 stub files exist and are recognized by Vitest
- npm test -- --run exits without crashing the runner (RED stubs are expected)
</success_criteria>

<output>
After completion, create `.planning/phases/06-slop-config-vault/06-01-SUMMARY.md`
</output>
