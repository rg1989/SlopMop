---
phase: 10-pty-session-persistence
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - client/components/SessionTabBar.tsx
  - client/App.css
autonomous: false
requirements:
  - PTY-04

must_haves:
  truths:
    - "A reconnecting tab shows a pulsing amber indicator distinct from the connecting (grey) and waiting (green) states"
    - "The status chip CSS class for a reconnecting session is 'status--reconnecting'"
    - "The composer is not blocked during reconnecting status — only 'connecting' blocks it"
  artifacts:
    - path: "client/components/SessionTabBar.tsx"
      provides: "STATUS_CLASS mapping includes reconnecting: 'status--reconnecting'"
      contains: "status--reconnecting"
    - path: "client/App.css"
      provides: ".status--reconnecting CSS rule with pulse animation"
      contains: "status--reconnecting"
  key_links:
    - from: "client/components/SessionTabBar.tsx"
      to: "client/hooks/useSessionManager.ts"
      via: "SessionStatus type import"
      pattern: "import.*SessionStatus.*useSessionManager"
    - from: "client/App.css"
      to: "client/components/SessionTabBar.tsx"
      via: ".status--reconnecting class applied to .status-chip span"
      pattern: "status--reconnecting"
---

<objective>
Add the 'reconnecting' visual state to the session tab bar and App.css. One-line change to SessionTabBar.tsx's STATUS_CLASS map, one CSS rule in App.css.

Purpose: Completes PTY-04 — users see a distinct amber pulsing indicator on tabs that are reconnecting to a live server-side PTY after reload. Turns the last RED test GREEN.
Output: Updated SessionTabBar.tsx and App.css; human-verified tab bar appearance; all tests green.
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pty-session-persistence/10-02-SUMMARY.md

@client/components/SessionTabBar.tsx
@client/App.css
@tests/SessionTabBar.test.tsx
</context>

<interfaces>
<!-- SessionTabBar.tsx STATUS_CLASS (current, line 14) — needs reconnecting entry -->
const STATUS_CLASS: Record<SessionStatus, string> = {
  connecting: 'status--connecting',
  waiting:    'status--waiting',
  working:    'status--working',
  done:       'status--done',
  error:      'status--error',
};

<!-- App.css — existing pulse animation for status--connecting (search for 'pulse') -->
/* The .status--connecting rule uses animation: pulse 1.2s infinite.
   .status--reconnecting should use the same animation but with --warning color. */

<!-- Design system rules (from CLAUDE.md) -->
/* --warning: #e3b341 */
/* --accent: #d4845a */
/* Use CSS custom properties only — no raw hex values */
/* Transition: animation: pulse 1s infinite (slightly faster than connecting at 1.2s) */
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reconnecting to STATUS_CLASS and App.css</name>
  <files>client/components/SessionTabBar.tsx, client/App.css</files>
  <behavior>
    - Test: 'renders status--reconnecting chip for reconnecting session (PTY-04)' — currently FAILS, must become GREEN
    - Test: existing 'tab has status class matching session status' — must remain GREEN
    - All other SessionTabBar tests — must remain GREEN
  </behavior>
  <action>
**client/components/SessionTabBar.tsx — one-line change:**

In the STATUS_CLASS Record (around line 14), add the reconnecting entry:

```typescript
const STATUS_CLASS: Record<SessionStatus, string> = {
  connecting:   'status--connecting',
  reconnecting: 'status--reconnecting',   // add this line
  waiting:      'status--waiting',
  working:      'status--working',
  done:         'status--done',
  error:        'status--error',
};
```

TypeScript will enforce completeness — if SessionStatus in useSessionManager.ts now includes 'reconnecting', omitting it from STATUS_CLASS causes a TS error. Adding it here fixes that error.

**client/App.css — add one CSS rule:**

Find the existing `.status--connecting` rule in App.css. Add `.status--reconnecting` immediately below it:

```css
.status--reconnecting { background: var(--warning); animation: pulse 1s infinite; }
```

Use `var(--warning)` (amber `#e3b341`) — distinct from connecting (grey `--txt-dim`) and waiting (green `--success`). The pulse animation is defined earlier in App.css under `@keyframes pulse` — reuse it. The 1s duration is slightly faster than connecting (1.2s) to convey active reconnection in progress.

Do NOT add `font-family` declarations. Do NOT use raw hex values. Follow CLAUDE.md design system rules.

Do NOT change: composer lock logic in App.tsx. The composer lock checks `!cwd || !terminal` — it does NOT check session status. 'reconnecting' status correctly does not block the composer (per research Pitfall 6).
  </action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/SlopMop && npm run test -- --run --reporter=verbose 2>&1 | grep -E "SessionTabBar|PTY-04|reconnecting|status--reconnecting" | head -20</automated>
  </verify>
  <done>
    SessionTabBar.tsx STATUS_CLASS has reconnecting entry. App.css has .status--reconnecting rule. All SessionTabBar tests pass including the previously-RED PTY-04 test. Full suite: npm run test -- --run green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — browser reload reconnects to live PTY</name>
  <action>Run `npm run dev` in the project root. Follow the verification steps below in the browser.</action>
  <files></files>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/SlopMop && npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms browser reload reconnects to live PTY with amber pulsing chip and scrollback replay.</done>
  <what-built>
PTY session persistence — complete implementation:
- usePty sends persisted session UUID on reconnect (PTY-01)
- Server replays buffer on reconnect (PTY-02, server already correct)
- Sessions idle 30+ min are cleaned up (PTY-03, server already correct)
- Session tab bar shows pulsing amber 'reconnecting' state after reload (PTY-04)
- Exited sessions show 'done' on reconnect — no re-launch (PTY-05)
  </what-built>
  <how-to-verify>
1. Start the app: `npm run dev` in the project root
2. Open http://localhost:5173 in the browser
3. Select a working directory and let the Claude session start (status chip goes from grey to green/orange pulse)
4. Type a message and send it — confirm the session name updates in the tab
5. Reload the browser tab (Cmd+R)
6. Expected: the tab reappears with an amber pulsing chip (reconnecting), then transitions to waiting/working as the session replays buffer
7. Expected: the terminal shows output from before the reload (scrollback replay)
8. Open DevTools Network tab — confirm only ONE WebSocket connection opens (no duplicate fresh PTY)
9. Confirm the session UUID in the start message (WS frame) matches the UUID stored in localStorage under slopmop_active:%2F{your-cwd}
  </how-to-verify>
  <resume-signal>Type "approved" if the reconnect works correctly, or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
After Task 1:
```bash
cd /Users/rgv250cc/Documents/Projects/SlopMop && npm run test -- --run 2>&1 | tail -10
```
Expected: all tests pass. Full suite green.

After Task 2 (checkpoint):
Human confirms live reconnect behaviour in browser.
</verification>

<success_criteria>
- SessionTabBar.tsx STATUS_CLASS includes reconnecting: 'status--reconnecting'
- App.css has .status--reconnecting { background: var(--warning); animation: pulse 1s infinite; }
- npm run test -- --run: all tests green (zero failures)
- Human confirms: browser reload reconnects to live PTY, scrollback replays, reconnecting amber chip visible then transitions to waiting
</success_criteria>

<output>
After completion, create `.planning/phases/10-pty-session-persistence/10-03-SUMMARY.md`
</output>
