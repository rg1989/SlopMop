---
phase: 10-pty-session-persistence
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/usePty.test.ts
  - tests/useSessionManager.test.ts
  - tests/SessionTabBar.test.tsx
autonomous: true
requirements:
  - PTY-01
  - PTY-02
  - PTY-03
  - PTY-04
  - PTY-05

must_haves:
  truths:
    - "Running npm run test -- --run after Wave 0 produces RED failures for every new PTY-01..PTY-05 test case"
    - "Existing green tests remain green — Wave 0 only adds new failing tests"
    - "All 5 requirement IDs have at least one test case in the failing set"
  artifacts:
    - path: "tests/usePty.test.ts"
      provides: "RED tests for PTY-01 (persisted UUID in start message) and PTY-05 (session-ready → waiting transition)"
      contains: "reconnect.*persisted\|session-ready"
    - path: "tests/useSessionManager.test.ts"
      provides: "RED tests for PTY-04 (restoreForCwd sets reconnecting status)"
      contains: "reconnecting"
    - path: "tests/SessionTabBar.test.tsx"
      provides: "RED tests for PTY-04 (status--reconnecting CSS class rendered)"
      contains: "status--reconnecting"
  key_links:
    - from: "tests/usePty.test.ts"
      to: "client/hooks/usePty.ts"
      via: "import"
      pattern: "from.*usePty"
    - from: "tests/useSessionManager.test.ts"
      to: "client/hooks/useSessionManager.ts"
      via: "import"
      pattern: "from.*useSessionManager"
    - from: "tests/SessionTabBar.test.tsx"
      to: "client/components/SessionTabBar.tsx"
      via: "import"
      pattern: "from.*SessionTabBar"
---

<objective>
Write failing (RED) test cases for all five PTY persistence requirements. These tests will fail because the implementation does not exist yet — that is intentional. Wave 2 makes them green.

Purpose: Establish the acceptance criteria as executable code before any implementation runs. Follows the project's established Wave 0 RED pattern (see STATE.md decisions for Phase 02 and Phase 04).
Output: Three extended test files with new failing describe blocks, existing tests untouched.
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pty-session-persistence/10-RESEARCH.md

@client/hooks/usePty.ts
@client/hooks/useSessionManager.ts
@client/components/SessionTabBar.tsx
@tests/usePty.test.ts
@tests/useSessionManager.test.ts
@tests/SessionTabBar.test.tsx
</context>

<interfaces>
<!-- Current SessionStatus — both hooks currently export this exact union -->
export type SessionStatus = 'connecting' | 'waiting' | 'working' | 'done' | 'error';

<!-- Target SessionStatus after Wave 2 — tests assert this variant exists -->
export type SessionStatus = 'connecting' | 'reconnecting' | 'waiting' | 'working' | 'done' | 'error';

<!-- ws-handler already sends session-ready on reconnect (no server changes needed) -->
{ type: 'session-ready'; sessionId: string }

<!-- usePty effect deps (current) — line 122 -->
}, [cwd, terminal, reconnectKey]); // cols/rows intentionally excluded

<!-- restoreForCwd (current) — line 113 in useSessionManager.ts -->
const reconnecting: SessionEntry[] = saved.map(s => ({ ...s, status: 'connecting' as SessionStatus }));
<!-- After Wave 2: status must be 'reconnecting', not 'connecting' -->

<!-- STATUS_CLASS in SessionTabBar.tsx (current, line 14) -->
const STATUS_CLASS: Record<SessionStatus, string> = {
  connecting: 'status--connecting',
  waiting:    'status--waiting',
  working:    'status--working',
  done:       'status--done',
  error:      'status--error',
};
<!-- After Wave 2: must include reconnecting: 'status--reconnecting' -->
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Add RED tests to usePty.test.ts (PTY-01, PTY-05)</name>
  <files>tests/usePty.test.ts</files>
  <action>
Append a new describe block 'PTY session reconnect' at the end of tests/usePty.test.ts. Do NOT modify existing tests.

Add these failing test cases:

**PTY-01 — usePty sends the persisted session UUID on reconnect:**
- Test name: 'uses provided sessionId prop in start message (PTY-01)'
- Render hook with sessionId='fixed-uuid-1234' prop
- Simulate WS open
- Assert start message contains sessionId === 'fixed-uuid-1234'
- This test will PASS immediately (the hook already sends sessionId prop). It should be GREEN after Wave 0. That is fine — it documents the existing behaviour.

**PTY-01 — sessionId in effect deps (reconnect sends correct ID when prop arrives late):**
- Test name: 'reopens WebSocket with new sessionId when sessionId prop changes (PTY-01)'
- Render hook with sessionId='uuid-a', cwd='/tmp', terminal=mockTerminal
- Simulate WS open → WS receives start with sessionId='uuid-a'
- Rerender hook with sessionId='uuid-b'
- Assert a SECOND WebSocket was opened (MockWebSocket called twice)
- Assert the second start message contains sessionId='uuid-b'
- This will FAIL until sessionId is added to the usePty effect deps in Wave 2.

**PTY-05 — session-ready message transitions status from 'reconnecting' to 'waiting':**
- Test name: 'transitions status from reconnecting to waiting on session-ready (PTY-05)'
- Setup: onStatus spy, render hook with sessionId='some-id'
- Call onStatus('reconnecting') directly to simulate the state before WS open
- Simulate WS open, simulate message { type: 'session-ready', sessionId: 'some-id' }
- Assert onStatus was called with 'waiting' after session-ready
- This will FAIL until the session-ready handler is added in Wave 2.

The MockWebSocket infrastructure (wsInstances, MockWebSocket, etc.) is already at the top of the file — do not re-declare it. Just append the new describe block.
  </action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/SlopMop && npm run test -- --run --reporter=verbose 2>&1 | grep -E "PTY-01|PTY-05|reconnect|FAIL|PASS" | head -20</automated>
  </verify>
  <done>
New describe block 'PTY session reconnect' added. At least two of the three new tests FAIL (red). The PTY-01 prop-forwarding test may pass — that is acceptable. Existing usePty tests remain green.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Add RED tests to useSessionManager.test.ts (PTY-04)</name>
  <files>tests/useSessionManager.test.ts</files>
  <action>
Append a new describe block 'restoreForCwd' at the end of tests/useSessionManager.test.ts. Do NOT modify existing tests.

Add these failing test cases:

**PTY-04 — restoreForCwd sets status 'reconnecting' not 'connecting':**
- Test name: 'restoreForCwd sets session status to reconnecting (PTY-04)'
- Seed localStorage key slopmop_active:%2Ftmp with JSON: { sessions: [{ id: 'restored-id', name: 'Old', status: 'waiting', cwd: '/tmp', createdAt: 1000 }], activeId: 'restored-id' }
- Call result.current.restoreForCwd('/tmp')
- Assert result.current.sessions[0].status === 'reconnecting'
- This will FAIL until restoreForCwd is updated in Wave 2 (currently sets 'connecting').

**PTY-04 — restoreForCwd preserves session UUIDs exactly:**
- Test name: 'restoreForCwd preserves the original session id (PTY-04)'
- Seed same localStorage entry
- Call restoreForCwd('/tmp')
- Assert result.current.sessions[0].id === 'restored-id'
- This should PASS immediately (existing code preserves IDs). Documents required behaviour.

**PTY-04 — restoreForCwd restores activeId:**
- Test name: 'restoreForCwd restores activeId from localStorage (PTY-04)'
- Seed localStorage with activeId: 'restored-id'
- Call restoreForCwd('/tmp')
- Assert result.current.activeId === 'restored-id'
- This should PASS immediately (existing code restores activeId). Documents required behaviour.

Note: The localStorage key for active sessions uses encodeURIComponent(cwd). For cwd='/tmp', the key is slopmop_active:%2Ftmp. Seed it correctly in the test.
  </action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/SlopMop && npm run test -- --run --reporter=verbose 2>&1 | grep -E "PTY-04|restoreForCwd|reconnecting|FAIL|PASS" | head -20</automated>
  </verify>
  <done>
New describe block 'restoreForCwd' added. The 'reconnecting' test FAILS (red). The id-preservation and activeId tests pass. Existing tests remain green.
  </done>
</task>

<task type="tdd">
  <name>Task 3: Add RED tests to SessionTabBar.test.tsx (PTY-04 visual)</name>
  <files>tests/SessionTabBar.test.tsx</files>
  <action>
Append a new describe block 'reconnecting status' at the end of tests/SessionTabBar.test.tsx. Do NOT modify existing tests.

The local SessionStatus type at the top of this test file is currently:
  type SessionStatus = 'connecting' | 'waiting' | 'working' | 'done' | 'error';

Update this LOCAL type declaration (line 6 in the test file) to include 'reconnecting':
  type SessionStatus = 'connecting' | 'reconnecting' | 'waiting' | 'working' | 'done' | 'error';

This change only affects the test file's local type — it does not change the component. It allows the test to compile and pass a session with status 'reconnecting'.

Add this failing test case:

**PTY-04 — reconnecting status chip renders status--reconnecting class:**
- Test name: 'renders status--reconnecting chip for reconnecting session (PTY-04)'
- Create sessions array with 2 entries so the tab list renders (showTabs = sessions.length > 1):
  - session-1: status 'reconnecting'
  - session-2: status 'waiting'
- Render SessionTabBar with these sessions
- Assert container.querySelector('.status--reconnecting') is NOT null
- This will FAIL until STATUS_CLASS in SessionTabBar.tsx includes 'reconnecting' in Wave 2.
  </action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/SlopMop && npm run test -- --run --reporter=verbose 2>&1 | grep -E "PTY-04|reconnecting|status--reconnecting|FAIL|PASS" | head -20</automated>
  </verify>
  <done>
New describe block 'reconnecting status' added. The status--reconnecting test FAILS (red). Existing SessionTabBar tests remain green. Full suite: npm run test -- --run passes all pre-existing tests and fails only the new Wave 0 stubs.
  </done>
</task>

</tasks>

<verification>
After all three tasks:

```bash
cd /Users/rgv250cc/Documents/Projects/SlopMop && npm run test -- --run 2>&1 | tail -20
```

Expected output:
- All pre-existing tests: PASS
- New failing tests: 2-4 FAIL (the ones testing not-yet-implemented behaviour)
- Overall: test run exits with failures (expected — this is Wave 0 RED)
</verification>

<success_criteria>
- tests/usePty.test.ts has new describe block with 3 new tests; at least 1 fails
- tests/useSessionManager.test.ts has new describe block with 3 new tests; at least 1 fails
- tests/SessionTabBar.test.tsx has new describe block with 1 new test; it fails
- All pre-existing tests remain green
- npm run test -- --run output shows the new failing tests clearly named with PTY-0N identifiers
</success_criteria>

<output>
After completion, create `.planning/phases/10-pty-session-persistence/10-01-SUMMARY.md`
</output>
