---
phase: 12-bottom-panel-shell
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified: []
autonomous: false
requirements: [BPANEL-01, BPANEL-02, BPANEL-03, BPANEL-04, BPANEL-05]

must_haves:
  truths:
    - "The bottom panel tab bar is visible as a 32px strip at the bottom of the main session area"
    - "Clicking the toggle button opens the panel body with a visible resize handle above it"
    - "Dragging the resize handle grows or shrinks the panel without collapsing the terminal area"
    - "Closing the browser tab and reopening restores panel height and open/closed state"
  artifacts:
    - path: "client/App.tsx"
      provides: "Bottom panel shell wired and visible in browser"
    - path: "client/App.css"
      provides: "Styled bottom panel tab bar and body"
  key_links: []
---

<objective>
Human visual and functional verification that the bottom panel shell works correctly in the running application.

Purpose: Confirms drag-resize feel, visual styling, localStorage restore across reload — behaviors that unit tests cannot cover.
Output: Approved bottom panel shell. Phase 12 complete.
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-bottom-panel-shell/12-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Bottom panel shell in client/App.tsx and client/App.css:
    - .bottom-panel-tab-bar — 32px strip always visible at bottom of main session area
    - Toggle button (chevron) opens/closes the panel body
    - .resize-handle--h — horizontal drag handle above panel body when open
    - useDragResize('up') — drag the handle to grow/shrink panel height
    - localStorage persistence for open state and height
  </what-built>
  <how-to-verify>
    Start the dev server: npm run dev (or equivalent), open http://localhost:5173

    1. BPANEL-01 visual: Confirm a thin 32px horizontal bar appears at the bottom of the main terminal area (below the session panes). It should contain a chevron button on the right.

    2. BPANEL-02/03 toggle: Click the chevron button. The panel body should expand downward with a visible empty dark area. Click again — panel collapses. The tab bar strip stays visible both times.

    3. BPANEL-03 resize: Open the panel. Drag the horizontal handle (the thin strip directly above the panel body) upward — panel should grow. Drag downward — panel shrinks. Terminal area should absorb the remaining space. Panel should never exceed ~50% of window height.

    4. BPANEL-04 persistence: Open the panel, resize it slightly, then hard-reload (Cmd+Shift+R). The panel should reopen at the same height.

    5. BPANEL-05 closed state: Close the panel, hard-reload. Panel should remain closed (only the tab bar strip visible).

    6. Visual style check: Tab bar background matches other surface elements (dark, not jarring). Chevron icon is visible and changes direction (up when open, down when closed). No raw hex colors, no font regressions.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Human sign-off on all 6 visual/functional checks above.
</verification>

<success_criteria>
- Bottom panel tab bar visible as permanent 32px strip
- Toggle opens/closes panel body cleanly
- Drag-resize works without collapsing terminal area
- localStorage restores height and open state across hard reload
- Visual styling consistent with app design system
</success_criteria>

<output>
After completion, create `.planning/phases/12-bottom-panel-shell/12-02-SUMMARY.md`
</output>
