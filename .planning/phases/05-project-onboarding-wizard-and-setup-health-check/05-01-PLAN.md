---
phase: 05-project-onboarding-wizard-and-setup-health-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/OnboardingModal.test.tsx
  - tests/useProjectHealth.test.ts
  - tests/HealthStatusBar.test.tsx
autonomous: true
requirements:
  - ONBOARD-01
  - ONBOARD-02
  - ONBOARD-03
  - HEALTH-01
  - HEALTH-02
  - HEALTH-03

must_haves:
  truths:
    - "Three test files exist and all tests fail (RED) — no implementation yet"
    - "OnboardingModal tests assert render/suppress/dismiss contract"
    - "useProjectHealth tests assert loading→resolved state transitions"
    - "HealthStatusBar tests assert dot color mapping from health state"
  artifacts:
    - path: "tests/OnboardingModal.test.tsx"
      provides: "ONBOARD-01, ONBOARD-02, ONBOARD-03 test coverage"
      contains: "@ts-expect-error"
    - path: "tests/useProjectHealth.test.ts"
      provides: "HEALTH-01, HEALTH-02 test coverage"
      contains: "@ts-expect-error"
    - path: "tests/HealthStatusBar.test.tsx"
      provides: "HEALTH-03 test coverage"
      contains: "@ts-expect-error"
  key_links:
    - from: "tests/OnboardingModal.test.tsx"
      to: "client/components/OnboardingModal.tsx"
      via: "@ts-expect-error import (Wave 0 RED pattern)"
      pattern: "@ts-expect-error"
    - from: "tests/useProjectHealth.test.ts"
      to: "client/hooks/useProjectHealth.ts"
      via: "@ts-expect-error import"
      pattern: "@ts-expect-error"
---

<objective>
Create Wave 0 test scaffolds for Phase 5. Three test files are written with @ts-expect-error imports so Vitest runs RED without TypeScript blocking compilation. No implementation code is created in this plan.

Purpose: Establish the test contracts before any implementation. RED tests prove the contract is correct before GREEN.
Output: Three test files that fail with "cannot find module" errors.
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Wave 0 pattern established in this codebase — from Phase 02 and 04 decisions: -->
<!-- Use @ts-expect-error on non-existent imports so Vitest runs RED without TypeScript blocking compilation -->

From tests/useSessionManager.test.ts (established Wave 0 pattern):
```typescript
// @ts-expect-error — module does not exist yet (Wave 0 RED)
import { useSessionManager } from '../client/hooks/useSessionManager';
```

From tests/setup.ts (RTL + Vitest globals already configured):
```typescript
import '@testing-library/jest-dom';
// vi, describe, it, expect all available globally
```

Existing localStorage mock pattern (from tests/setup.ts or inline):
```typescript
// localStorage is available in jsdom test environment
// Use vi.stubGlobal or direct localStorage.setItem/removeItem in tests
```

Contracts to stub (modules don't exist yet):
- client/components/OnboardingModal.tsx — exports OnboardingModal component
- client/hooks/useProjectHealth.ts — exports useProjectHealth hook and ProjectHealth interface
- client/components/HealthStatusBar.tsx — exports HealthStatusBar component
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write OnboardingModal test scaffold (RED)</name>
  <files>tests/OnboardingModal.test.tsx</files>
  <action>
Create `tests/OnboardingModal.test.tsx` with @ts-expect-error import so it runs RED in Vitest.

Test contract to encode:

```typescript
// @ts-expect-error — module does not exist yet (Wave 0 RED)
import { OnboardingModal } from '../client/components/OnboardingModal';
```

Three test cases (use describe + it blocks):

1. **ONBOARD-01 — renders when no saved folder + not yet onboarded:**
   - Clear `localStorage.removeItem('slopmop_onboarded')` in beforeEach
   - Render `<OnboardingModal initialPath={null} onDismiss={vi.fn()} />`
   - Expect heading text "Welcome to SlopMop" to be in the document

2. **ONBOARD-02 — does NOT render when initialPath is set:**
   - `localStorage.removeItem('slopmop_onboarded')`
   - Render `<OnboardingModal initialPath="/some/path" onDismiss={vi.fn()} />`
   - Expect the modal to NOT render (container should be null or empty — check `queryByText('Welcome to SlopMop')` returns null)

3. **ONBOARD-03 — dismissing modal sets localStorage key:**
   - `localStorage.removeItem('slopmop_onboarded')`
   - Render `<OnboardingModal initialPath={null} onDismiss={vi.fn()} />`
   - Click the "Get Started" button (use `getByRole('button', { name: /get started/i })`)
   - Expect `localStorage.getItem('slopmop_onboarded')` to equal `'1'`

Import `render`, `screen`, `fireEvent` from `@testing-library/react`. Import `vi` from `vitest`.

The test file must include the @ts-expect-error comment immediately before the import line so TypeScript suppresses the module-not-found error but Vitest still runs and fails at runtime.
  </action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/SlopMop && npm test -- --run OnboardingModal 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists, Vitest runs it, all 3 tests FAIL with "cannot find module" or similar import error — NOT with "no tests found"</done>
</task>

<task type="auto">
  <name>Task 2: Write useProjectHealth and HealthStatusBar test scaffolds (RED)</name>
  <files>tests/useProjectHealth.test.ts, tests/HealthStatusBar.test.tsx</files>
  <action>
Create two more Wave 0 test files.

**tests/useProjectHealth.test.ts:**

```typescript
// @ts-expect-error — module does not exist yet (Wave 0 RED)
import { useProjectHealth } from '../client/hooks/useProjectHealth';
```

Two test cases:

1. **HEALTH-01 — returns loading:true then resolves with health data:**
   - Mock `global.fetch` with `vi.fn().mockResolvedValue({ json: () => Promise.resolve({ dirAccessible: true, isGitRepo: true, hasClaudeMd: false, agentFound: true, agentPath: '/usr/local/bin/claude', hasNodeModules: null }) })`
   - Use `renderHook(() => useProjectHealth('/some/path'))` from `@testing-library/react`
   - On initial render, `result.current.loading` should be `true`
   - After `waitFor`, `result.current.loading` should be `false` and `result.current.isGitRepo` should be `true`

2. **HEALTH-02 — resets to loading when cwd changes:**
   - Mock fetch to return valid health JSON
   - Use `renderHook` with `cwd` as a variable, start with `'/path/one'`
   - After first resolution, rerender with `'/path/two'`
   - Immediately after rerender, `result.current.loading` should be `true` again

Import `renderHook`, `waitFor`, `act` from `@testing-library/react`. Import `vi`, `beforeEach`, `afterEach` from `vitest`. Restore fetch mock in `afterEach`.

---

**tests/HealthStatusBar.test.tsx:**

```typescript
// @ts-expect-error — module does not exist yet (Wave 0 RED)
import { HealthStatusBar } from '../client/components/HealthStatusBar';
```

One test case:

1. **HEALTH-03 — renders correct dot count and aria-labels per health state:**
   - Create a mock health object: `{ loading: false, dirAccessible: true, isGitRepo: false, hasClaudeMd: false, agentFound: true, agentPath: '/usr/bin/claude', hasNodeModules: null }`
   - Render `<HealthStatusBar health={mockHealth} />`
   - Expect the component to render at least one element with class `health-dot--warn` (for isGitRepo=false) and one with class `health-dot--ok` (for agentFound=true)
   - When loading=true, dots should have class `health-dot--loading`

Use `render`, `screen`, `container` from `@testing-library/react`.
  </action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/SlopMop && npm test -- --run useProjectHealth HealthStatusBar 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist and run RED in Vitest — import errors confirm modules don't exist yet</done>
</task>

</tasks>

<verification>
Run full suite to confirm new tests fail RED and existing tests remain GREEN:

```bash
cd /Users/rgv250cc/Documents/Projects/SlopMop && npm test -- --run 2>&1 | tail -30
```

Expected: 3 new test files FAIL (module not found), all pre-existing tests PASS. Total failures = exactly the new Wave 0 tests.
</verification>

<success_criteria>
- tests/OnboardingModal.test.tsx exists with 3 test cases covering ONBOARD-01, ONBOARD-02, ONBOARD-03
- tests/useProjectHealth.test.ts exists with 2 test cases covering HEALTH-01, HEALTH-02
- tests/HealthStatusBar.test.tsx exists with 1 test case covering HEALTH-03
- All 3 files use @ts-expect-error on the non-existent module imports
- Vitest runs all 3 files and fails at import (not "no tests found")
- All pre-existing tests remain GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-onboarding-wizard-and-setup-health-check/05-01-SUMMARY.md`
</output>
