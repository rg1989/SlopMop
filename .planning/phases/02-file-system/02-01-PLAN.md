---
phase: 02-file-system
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/FileTree.test.tsx
  - tests/useFileTree.test.ts
  - tests/FilePreview.test.tsx
autonomous: true
requirements:
  - FILE-01
  - FILE-02
  - FILE-03
  - FILE-04
  - FILE-05

must_haves:
  truths:
    - "All test files exist and run without syntax errors (even though tests fail red at this stage)"
    - "Every FILE-01 through FILE-05 behavior has a corresponding failing test"
    - "Composer @path injection test cases are added to the existing Composer test file"
  artifacts:
    - path: "tests/FileTree.test.tsx"
      provides: "Unit tests for FILE-01, FILE-02, FILE-03, FILE-05"
      exports: ["FileTree renders nodes", "toggle dir", "changedPaths", "select file", "preview click"]
    - path: "tests/useFileTree.test.ts"
      provides: "Unit tests for useFileTree hook (FILE-02)"
      exports: ["changes mode", "fetch on cwd change"]
    - path: "tests/FilePreview.test.tsx"
      provides: "Unit tests for FilePreview component (FILE-03, FILE-05)"
      exports: ["text preview", "image preview"]
    - path: "tests/Composer.test.tsx"
      provides: "Extended with @path injection cases (FILE-04)"
      contains: "@path injection"
  key_links:
    - from: "tests/FileTree.test.tsx"
      to: "client/components/FileTree.tsx"
      via: "import"
      pattern: "from.*FileTree"
    - from: "tests/useFileTree.test.ts"
      to: "client/hooks/useFileTree.ts"
      via: "import"
      pattern: "from.*useFileTree"
    - from: "tests/FilePreview.test.tsx"
      to: "client/components/FilePreview.tsx"
      via: "import"
      pattern: "from.*FilePreview"
---

<objective>
Create the Wave 0 test scaffolds for Phase 2. All tests are written in RED state (they import modules that do not yet exist). The test files define the behavioral contract that implementation plans must satisfy.

Purpose: Establish the Nyquist sampling contract before any implementation begins. Executors in later waves implement against these explicit expectations.
Output: 3 new test files + 4 new test cases in existing Composer.test.tsx.
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-system/02-RESEARCH.md
@.planning/phases/02-file-system/02-VALIDATION.md
@tests/Composer.test.tsx
@vitest.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FileTree and useFileTree test scaffolds</name>
  <files>tests/FileTree.test.tsx, tests/useFileTree.test.ts</files>
  <behavior>
    FileTree tests (these will fail until 02-03 is executed):
    - "renders file tree" — renders FileTree with a minimal JSON tree; confirms directory name and file name appear in DOM
    - "toggle dir" — clicking a directory header collapses it (children no longer visible); clicking again expands
    - "changedPaths highlights changed file" — when changedPaths Set contains a file's abs path, that file element has class ft-changed
    - "select file adds to selection" — double-clicking a file calls onSelect with the absolute path
    - "preview click" — single-clicking a file calls onPreview with the absolute path

    useFileTree tests (these will fail until 02-03 is executed):
    - "fetches tree on cwd change" — when cwd changes, fetch is called with /api/files?cwd=... and tree state is updated
    - "setMode to changes calls git-status" — calling setMode('changes') triggers fetch to /api/git-status?cwd=...
    - "setMode to all does not call git-status" — switching back to 'all' does NOT re-fetch git-status
  </behavior>
  <action>
    Create tests/FileTree.test.tsx:
    - Import FileTree from '../client/components/FileTree' (file does not exist yet — import will fail until 02-03)
    - Import FileNode type from '../client/components/FileTree'
    - Build a minimal test tree fixture: one dir node containing two file nodes (use absolute paths like /test/project/src and /test/project/src/index.ts, /test/project/src/App.tsx)
    - Use @testing-library/react render + screen + fireEvent
    - The FileTree component will receive props: `nodes` (FileNode[]), `selected` (Set<string>), `onSelect` (fn), `onPreview` (fn), `changedPaths` (Set<string>)
    - All 5 tests listed in behavior block

    Create tests/useFileTree.test.ts:
    - Import useFileTree from '../client/hooks/useFileTree'
    - Mock global fetch using vi.fn() in beforeEach
    - Use renderHook from @testing-library/react
    - The hook signature: useFileTree(cwd: string | null) returns { tree, changedPaths, mode, setMode }
    - All 3 tests listed in behavior block; use vi.fn for fetch mock and waitFor for async state updates

    NOTE: Tests import modules that don't exist yet. TypeScript will complain but Vitest can run the tests and they will fail with module-not-found. That is the expected RED state. Write the tests as if the modules already exist with the correct signatures.
  </action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/ClaudeTalk && npm test -- tests/FileTree.test.tsx tests/useFileTree.test.ts 2>&1 | tail -20; echo "EXIT:$?"</automated>
  </verify>
  <done>Both test files exist; running the suite shows test failures with "Cannot find module" or assertion errors (RED state confirmed by FAIL or "Cannot find module" in output). Zero syntax errors in the test files themselves (vitest parses and attempts to run them). Verify command exits 0 — RED state is the expected outcome, not a fatal error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FilePreview test scaffold and extend Composer tests</name>
  <files>tests/FilePreview.test.tsx, tests/Composer.test.tsx</files>
  <behavior>
    FilePreview tests (will fail until 02-04 is executed):
    - "renders text content" — given a text file response { type: 'text', content: 'hello world' }, the component renders the text in a pre/code element
    - "renders image tag for binary image" — given { type: 'binary', isImage: true, base64: 'abc', ext: '.png' }, renders an img element with src="data:image/png;base64,abc"
    - "renders binary message for non-image binary" — given { type: 'binary', isImage: false, ext: '.zip' }, renders a "Binary file" notice (no img tag)
    - "renders nothing when content is null" — given no file selected (null prop), renders null/empty

    Composer @path injection tests (extend existing tests/Composer.test.tsx):
    - "@path injection — single attachment" — Composer receives attachments=['/abs/path/file.ts']; user types 'hello' and presses Enter; onSend called with '@/abs/path/file.ts\nhello\r'
    - "@path injection — multiple attachments" — attachments=['/a/b.ts', '/c/d.ts']; onSend called with '@/a/b.ts @/c/d.ts\nhello\r'
    - "@path injection — no attachments" — attachments=[] (empty); onSend called with 'hello\r' (no @paths prepended, existing behavior preserved)
    - "attachments cleared after send" — a clearAttachments prop fn is called once after Enter is pressed
  </behavior>
  <action>
    Create tests/FilePreview.test.tsx:
    - Import FilePreview from '../client/components/FilePreview'
    - Import FilePreviewData type (the union type the component accepts as prop)
    - Use @testing-library/react render + screen
    - The component signature: FilePreview({ data }: { data: FilePreviewData | null })
    - All 4 tests listed in behavior block

    Extend tests/Composer.test.tsx:
    - Add 4 new test cases to the existing 'Composer' describe block
    - The modified Composer will accept two new optional props: attachments?: string[] and clearAttachments?: () => void
    - When attachments has entries, onSend receives '@path1 @path2\nmessage\r'
    - When attachments is empty/undefined, behavior is identical to existing tests
    - clearAttachments is called once after send when attachments is non-empty
    - Do NOT modify existing test cases — only append new ones

    NOTE: FilePreview imports a module that doesn't exist yet (RED state). Composer tests extend the existing file — the new tests exercise props not yet implemented.
  </action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/ClaudeTalk && npm test -- tests/FilePreview.test.tsx tests/Composer.test.tsx 2>&1 | tail -25; echo "EXIT:$?"</automated>
  </verify>
  <done>FilePreview.test.tsx exists; Composer.test.tsx has 4 new @path test cases. New FilePreview tests fail RED (module not found or assertion failure — confirmed by FAIL or "Cannot find module" in output). All ORIGINAL Composer tests still pass GREEN (existing behavior untouched). Verify command exits 0 — RED state for new tests is expected.</done>
</task>

</tasks>

<verification>
Run full suite: `cd /Users/rgv250cc/Documents/Projects/ClaudeTalk && npm test 2>&1 | tail -30`

Expected: Original tests (Composer, pty-manager, usePty, useResize) continue to pass. New test files exist and fail with expected module-not-found or assertion errors (FAIL or "Cannot find module" visible in output confirms RED state).

IMPORTANT: The verify commands intentionally exit 0 even when tests fail RED. RED test output is the success condition for Wave 0 — the executor MUST NOT abort when seeing FAIL output from these test files.
</verification>

<success_criteria>
- tests/FileTree.test.tsx exists with 5 test cases covering FILE-01, FILE-02, FILE-03, FILE-05
- tests/useFileTree.test.ts exists with 3 test cases covering FILE-02 hook behavior
- tests/FilePreview.test.tsx exists with 4 test cases covering FILE-03, FILE-05 rendering
- tests/Composer.test.tsx extended with 4 @path injection test cases covering FILE-04
- All pre-existing tests remain GREEN
- New test files are in RED state (implementation not yet built)
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-system/02-01-SUMMARY.md`
</output>
