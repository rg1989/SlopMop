---
phase: 11-canvas-panel-extraction
plan: 03
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - client/App.css
autonomous: true
requirements:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-03

must_haves:
  truths:
    - "canvas-column CSS class gives the right column the correct flex layout and border"
    - "canvas-column-header CSS class gives the header bar the correct height, padding, and surface background"
    - "canvas-column-label CSS class applies the 11px uppercase label style from the design system"
    - "canvas-toggle-btn CSS class matches the existing fp-btn / toolbar button pattern"
    - "All colors use CSS variables from theme.css — no raw hex values"
  artifacts:
    - path: "client/App.css"
      provides: "CSS classes for canvas-column, canvas-column-header, canvas-column-label, canvas-toggle-btn"
      contains: ".canvas-column"
  key_links:
    - from: ".canvas-column"
      to: "flex-shrink: 0"
      via: "prevents column from collapsing when terminal area needs more space"
      pattern: "flex-shrink.*0"
    - from: ".canvas-toggle-btn"
      to: "var(--txt-dim) / var(--surface-hover)"
      via: "color/background transitions matching existing toolbar button pattern"
      pattern: "canvas-toggle-btn"
---

<objective>
Add CSS classes for the canvas column to client/App.css. This plan runs in parallel with 11-02 (which touches only App.tsx — no file conflict).

Purpose: The canvas column JSX added in 11-02 needs its CSS classes. These can be written independently since App.css and App.tsx are separate files.
Output: Four new CSS rule blocks in client/App.css.
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-canvas-panel-extraction/11-01-SUMMARY.md
@.planning/PROJECT.md

<interfaces>
<!-- Design system rules from CLAUDE.md — must follow exactly -->

Color variables (from client/theme.css — use only these):
- --bg: #0d1117  (column background)
- --surface: #161b22  (header background)
- --surface-hover: #1c2128  (button hover)
- --border: #30363d  (borders)
- --border-muted: #484f58  (label color)
- --txt-dim: #6e7681  (button default color)
- --txt: #c9d1d9  (button hover color)
- --accent: #d4845a  (resize handle active)
- --accent-rgb: 212, 132, 90

Font sizes: 11px for label (with uppercase + letter-spacing)
Spacing: 6px 10px for header padding
Border radius: 3px for buttons
Transitions: color 0.12s, background 0.12s

Existing similar classes to match style:
- .editor-panel: flex-shrink:0; overflow:hidden; background:var(--bg); border-left:1px solid var(--border); display:flex; flex-direction:column
- FolderPicker buttons: background:none; border:none; cursor:pointer; padding:2px 4px; border-radius:3px

Existing resize handle:
- .resize-handle: width:4px; cursor:col-resize; flex-shrink:0; background:var(--border); transition:background 0.15s
- .resize-handle:hover, .resize-handle.dragging: background:var(--accent)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canvas column CSS classes to App.css</name>
  <files>client/App.css</files>
  <action>
Append the following CSS block to client/App.css, after the existing `.editor-panel` block (around line 3950). Place it in a new section comment "/* Canvas column — persistent right panel (Phase 11) */":

```css
/* Canvas column — persistent right panel (Phase 11) */
.canvas-column {
  flex-shrink: 0;
  overflow: hidden;
  background: var(--bg);
  border-left: 1px solid var(--border);
  display: flex;
  flex-direction: column;
}

.canvas-column-header {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 6px 10px;
  border-bottom: 1px solid var(--border);
  background: var(--surface);
}

.canvas-column-label {
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--border-muted);
}

.canvas-toggle-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  background: none;
  border: none;
  cursor: pointer;
  color: var(--txt-dim);
  padding: 2px 4px;
  border-radius: 3px;
  transition: color 0.12s, background 0.12s;
}
.canvas-toggle-btn:hover {
  color: var(--txt);
  background: var(--surface-hover);
}
```

Do NOT add font-family declarations — global `*` rule covers it.
Do NOT use raw hex values — all colors are CSS variables.
The canvas-column must get `flex-shrink: 0` so it doesn't collapse when the terminal area expands (matches editor-panel behavior).

The `.live-canvas-panel` inside `.canvas-column` must fill remaining height. Check the existing `.live-canvas-panel` CSS rule — if it already has `flex: 1` or `height: 100%`, no change needed. If not, add:
```css
.canvas-column .live-canvas-panel {
  flex: 1;
  overflow: hidden;
}
```
</action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/SlopMop && grep -n "canvas-column" client/App.css | head -20</automated>
  </verify>
  <done>.canvas-column, .canvas-column-header, .canvas-column-label, .canvas-toggle-btn CSS rules exist in App.css. No raw hex values. No font-family declarations (except the two allowed exception values if needed).</done>
</task>

</tasks>

<verification>
Confirm no raw hex colors introduced: `grep -n "#[0-9a-fA-F]\{3,6\}" /Users/rgv250cc/Documents/Projects/SlopMop/client/App.css | grep "canvas-column"` — should return empty.

Confirm full test suite still passes (no CSS import errors): `cd /Users/rgv250cc/Documents/Projects/SlopMop && npm test -- --run 2>&1 | tail -10`
</verification>

<success_criteria>
- client/App.css contains .canvas-column, .canvas-column-header, .canvas-column-label, .canvas-toggle-btn rules
- All colors use CSS variables — no raw hex values in the new block
- .canvas-column has flex-shrink: 0 and flex-direction: column
- npm test suite passes (CSS changes don't break any tests)
</success_criteria>

<output>
After completion, create `.planning/phases/11-canvas-panel-extraction/11-03-SUMMARY.md`
</output>
