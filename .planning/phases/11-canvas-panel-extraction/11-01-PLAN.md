---
phase: 11-canvas-panel-extraction
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/App.canvasPanel.test.tsx
  - tests/LiveCanvasColumn.test.tsx
autonomous: true
requirements:
  - CANVAS-01
  - CANVAS-02
  - CANVAS-03
  - CANVAS-04
  - CANVAS-05

must_haves:
  truths:
    - "Five failing tests exist for canvas panel extraction behavior"
    - "Tests reference modules/behavior that does not yet exist — RED confirmed by running npm test"
    - "Test file for sidebar tab removal (CANVAS-04, CANVAS-05) exists at tests/App.canvasPanel.test.tsx"
    - "Test file for canvas column visibility and toggle (CANVAS-01, CANVAS-02, CANVAS-03) exists at tests/LiveCanvasColumn.test.tsx"
  artifacts:
    - path: "tests/App.canvasPanel.test.tsx"
      provides: "RED tests for CANVAS-04 (sidebar has 4 tabs) and CANVAS-05 (canvas width restored from localStorage)"
    - path: "tests/LiveCanvasColumn.test.tsx"
      provides: "RED tests for CANVAS-01 (renders when visible), CANVAS-02 (hidden when not visible), CANVAS-03 (toggle persists to localStorage)"
  key_links:
    - from: "tests/App.canvasPanel.test.tsx"
      to: "client/App.tsx"
      via: "import App component, render and inspect sidebar tabs"
      pattern: "SIDEBAR_TABS.*length.*4"
    - from: "tests/LiveCanvasColumn.test.tsx"
      to: "canvas column behavior in App.tsx"
      via: "render App with mocked localStorage, assert canvas column presence"
      pattern: "canvas-column"
---

<objective>
Create two RED test files covering the five canvas panel extraction behaviors. Tests must fail (RED) because the implementation does not yet exist.

Purpose: Nyquist compliance — every behavior in Phase 11 has a failing automated test before implementation begins.
Output: tests/App.canvasPanel.test.tsx and tests/LiveCanvasColumn.test.tsx, both RED.
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Extracted from client/App.tsx — current canvas/sidebar state -->

From client/App.tsx:
```typescript
type SidebarTabId = 'explorer' | 'changes' | 'roadmap' | 'brain' | 'canvas';

const SIDEBAR_TABS: { id: SidebarTabId; label: string; Icon: () => JSX.Element }[] = [
  { id: 'explorer', ... },
  { id: 'changes', ... },
  { id: 'roadmap', ... },
  { id: 'brain', ... },
  { id: 'canvas', ... },   // <-- currently 5 tabs, Phase 11 removes this
];

// localStorage keys to be added in Phase 11:
// UI.canvasVisible = 'slopmop_ui:canvas_visible'
// UI.canvasWidth   = 'slopmop_ui:canvas_width'
```

From client/components/LiveCanvasPanel.tsx:
```typescript
interface LiveCanvasPanelProps {
  cwd: string;
  isDragging?: boolean;
}
export const LiveCanvasPanel: FC<LiveCanvasPanelProps>
```

From tests/setup.ts (existing test infrastructure):
- jsdom environment configured
- @testing-library/react available
- vitest globals (describe, it, expect, beforeEach, vi) available
- localStorage not auto-mocked — use vi.stubGlobal or direct localStorage manipulation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for sidebar tab removal and canvas width restore (App.canvasPanel)</name>
  <files>tests/App.canvasPanel.test.tsx</files>
  <behavior>
    - CANVAS-04: When App renders with a connected cwd, the sidebar tab bar contains exactly 4 tab buttons (explorer, changes, roadmap, brain) — not 5. The canvas tab is gone.
    - CANVAS-05: When localStorage has 'slopmop_ui:canvas_width' set to 450, the canvas column renders at width 450px (or at least the canvas column element is present, because the width is read from localStorage on mount).
  </behavior>
  <action>
Create tests/App.canvasPanel.test.tsx. Mock all heavy dependencies (useDragResize returns {width: 240, isDragging: false, onMouseDown: vi.fn(), setWidth: vi.fn()}, useSessionManager returns minimal stub, useFileTree returns {tree:[], changedPaths:[], gitStatus:null, loadChanges:vi.fn(), mode:'all', setMode:vi.fn()}, useAudioCoordinator returns stub, useSettings returns stub with agent config, useProjectHealth returns empty array). Mock fetch to return {exists:true} for /api/dir-exists and {exists:true} for /api/slop-status.

CANVAS-04 test: render App, find all elements with role="button" that are sidebar tab buttons (use `.stab` class selector or count buttons in `.sidebar-tabs-bar`). Assert count is 4, not 5. Assert no button with title "Live Canvas" exists.

CANVAS-05 test: before render, set localStorage 'slopmop_ui:canvas_width' to JSON.stringify(450). Render App with a cwd set. Assert a `.canvas-column` element exists in the document. (This test fails RED because canvas-column does not exist yet.)

Both tests must be RED (failing) when run against the current codebase. Use @ts-expect-error only if TypeScript blocks compilation, not for runtime assertions.

Run after writing: `npm test -- --run App.canvasPanel` — confirm RED output (tests fail, not error out).</action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/SlopMop && npm test -- --run App.canvasPanel 2>&1 | tail -20</automated>
  </verify>
  <done>Both CANVAS-04 and CANVAS-05 tests exist and fail RED. npm test output shows test failures (not compilation errors) for App.canvasPanel.test.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED tests for canvas column visibility and toggle (LiveCanvasColumn)</name>
  <files>tests/LiveCanvasColumn.test.tsx</files>
  <behavior>
    - CANVAS-01: When App renders with cwd set and isCanvasVisible is true (default), a `.canvas-column` element is present in the DOM.
    - CANVAS-02: When App renders with localStorage 'slopmop_ui:canvas_visible' = false, the `.canvas-column` element is NOT in the DOM.
    - CANVAS-03: Clicking the canvas toggle button (canvas-toggle-btn or toolbar toggle in FolderPicker) calls uiWrite with 'slopmop_ui:canvas_visible' and flips visibility.
  </behavior>
  <action>
Create tests/LiveCanvasColumn.test.tsx. Use the same App-level mock setup as Task 1 (copy the vi.mock boilerplate for useDragResize, useSessionManager, useFileTree, useAudioCoordinator, useSettings, useProjectHealth, fetch).

CANVAS-01 test: render App with cwd. Assert document.querySelector('.canvas-column') is not null.

CANVAS-02 test: before render, set localStorage 'slopmop_ui:canvas_visible' = JSON.stringify(false). Render App. Assert document.querySelector('.canvas-column') is null.

CANVAS-03 test: render App with cwd (canvas visible). Find the canvas toggle button (data-testid="canvas-toggle-btn" or className matching "canvas-toggle-btn"). Use fireEvent.click. Assert .canvas-column disappears from DOM. Assert localStorage.getItem('slopmop_ui:canvas_visible') equals JSON.stringify(false).

All three tests must be RED when run against current codebase. The canvas-column does not exist — every test that asserts its presence fails, every test that asserts its absence passes (so CANVAS-02 accidentally passes — that is acceptable for Wave 0 stubs; the RED requirement applies to the set overall).

Run after writing: `npm test -- --run LiveCanvasColumn` — confirm test failures for the positive assertions.</action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/SlopMop && npm test -- --run LiveCanvasColumn 2>&1 | tail -20</automated>
  </verify>
  <done>CANVAS-01 and CANVAS-03 tests fail RED (canvas-column not found). CANVAS-02 may pass (correctly asserts absence). npm test on both files runs without compilation errors.</done>
</task>

</tasks>

<verification>
Run full suite to confirm no regressions: `cd /Users/rgv250cc/Documents/Projects/SlopMop && npm test -- --run 2>&1 | tail -30`

Pre-existing tests must remain green. The two new test files introduce failures only for canvas-specific assertions.
</verification>

<success_criteria>
- tests/App.canvasPanel.test.tsx exists with CANVAS-04 and CANVAS-05 tests
- tests/LiveCanvasColumn.test.tsx exists with CANVAS-01, CANVAS-02, CANVAS-03 tests
- At least 3 of the 5 tests are RED (failing) because the implementation doesn't exist yet
- All pre-existing tests remain green
- npm test runs without TypeScript compilation errors in the new files
</success_criteria>

<output>
After completion, create `.planning/phases/11-canvas-panel-extraction/11-01-SUMMARY.md`
</output>
