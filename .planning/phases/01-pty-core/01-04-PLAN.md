---
phase: 01-pty-core
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified: []
autonomous: false
requirements:
  - TERM-04

must_haves:
  truths:
    - "User can scroll back through terminal history (mouse scroll or keyboard)"
    - "User can select text in the terminal and copy with Cmd+C on macOS"
    - "Ctrl+C inside the terminal sends SIGINT to the PTY process (not clipboard copy)"
    - "Claude CLI opens in the selected folder with full color output and interactive prompts"
    - "Multiline messages composed with Shift+Enter are sent and executed correctly"
    - "Terminal resizes correctly when the browser window is resized"
  artifacts: []
  key_links: []
---

<objective>
End-to-end manual verification of the complete Phase 1 implementation. Confirms all five phase success criteria are met before phase sign-off.

Purpose: TERM-04 (scrollback + copy/paste) cannot be tested automatically — it requires real browser interaction and the Clipboard API. This checkpoint validates the full PTY loop including Claude CLI behavior in a real browser.
Output: Human-confirmed phase completion; any issues surfaced for gap closure.
</objective>

<execution_context>
@/Users/rgv250cc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rgv250cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-pty-core/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev environment and run full automated suite</name>
  <files></files>
  <action>
1. Run the complete automated test suite:
   ```
   npx vitest run
   ```
   All tests must be green. If any fail, fix them before proceeding to the checkpoint.

2. Start the development server:
   ```
   npm run dev
   ```
   Wait for both the backend (port 3000) and the Vite dev server (typically port 5173) to confirm startup messages.

3. Open `http://localhost:5173` in the browser and confirm the app loads without console errors.
  </action>
  <verify>
    <automated>cd /Users/rgv250cc/Documents/Projects/ClaudeTalk && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>All automated tests pass. Dev server starts and app loads in browser with no console errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end manual verification (TERM-01 through TERM-05)</name>
  <files></files>
  <action>Human verifies all 5 phase success criteria in the running browser app at http://localhost:5173</action>
  <verify>Human confirms via resume-signal below</verify>
  <done>User types "approved" — all TERM-01 through TERM-05 behaviors confirmed working in real browser</done>

  <what-built>
    Complete Phase 1 PTY Core implementation:
    - Node.js backend with Express + WebSocket + node-pty
    - xterm.js terminal with WebGL renderer and FitAddon
    - Multiline composer (Enter sends, Shift+Enter newlines)
    - Folder picker to set the Claude CLI working directory
    - Debounced terminal resize propagated to PTY

    All automated tests green (pty-manager, useResize, usePty, Composer).
    TERM-04 (scrollback + copy/paste) requires manual browser verification.
  </what-built>
  <how-to-verify>
    App is running at http://localhost:5173. Verify each success criterion:

    **TERM-01: Folder selection + Claude CLI session**
    1. Enter a real project folder path in the folder input (e.g. `/Users/your-name/Desktop/test`)
    2. Click Connect
    3. Confirm Claude CLI starts in the terminal panel — you should see the Claude CLI prompt

    **TERM-02: ANSI colors + interactive prompts**
    4. Confirm Claude CLI renders with colors (not plain text)
    5. Send a simple message and confirm the response renders with formatting

    **TERM-03: Multiline composer**
    6. In the composer, type line 1, press Shift+Enter — confirm a newline is added (NOT sent yet)
    7. Type line 2, press Enter — confirm the full multiline text is sent to Claude CLI
    8. Confirm the composer textarea clears after sending

    **TERM-04: Scrollback + copy/paste (manual only)**
    9. Generate enough output to fill the terminal (e.g. send "ls -la /usr/local")
    10. Scroll up with the mouse — confirm history is visible above the current prompt
    11. Select some terminal text by clicking and dragging
    12. Press Cmd+C — confirm text is copied (paste into another app to verify)
    13. Press Ctrl+C inside the terminal — confirm it sends interrupt (not clipboard copy)

    **TERM-05: Terminal resize**
    14. Resize the browser window narrower and wider
    15. Confirm the terminal reflows correctly (text wraps at new width, no garbled output)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all criteria pass.
    If issues found, describe them (e.g., "TERM-02 failing: no colors") and run `/gsd:plan-phase --gaps` to create gap closure plans.
  </resume-signal>
</task>

</tasks>

<verification>
Human-confirmed via checkpoint:
- TERM-01: Claude CLI opens in selected directory
- TERM-02: Full ANSI color rendering
- TERM-03: Multiline composer works correctly
- TERM-04: Scrollback and Cmd+C copy work, Ctrl+C sends SIGINT
- TERM-05: Window resize causes correct terminal reflow
</verification>

<success_criteria>
All 5 phase success criteria from ROADMAP.md confirmed by user. Phase 1 PTY Core is complete. Phase 2 File System planning can begin.
</success_criteria>

<output>
After completion, create `.planning/phases/01-pty-core/01-04-SUMMARY.md` following the summary template.
Update ROADMAP.md Phase 1 status to complete.
</output>
